import { View, Text } from "react-native";

export default function SponsoredChallenge() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Sponsored Challenge Screen</Text>
    </View>
  );
}
